using UnityEngine;

public class OtherPlayerInfoUIManager : MonoBehaviour
{
    [SerializeField]
    private GameObject namePanelObj;
    [SerializeField]
    private GameObject nameTextObj;
    [SerializeField]
    private GameObject handCountPanelObj;
    [SerializeField]
    private GameObject handCountTextObj;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
